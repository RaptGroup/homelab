[Unit]
Description=Nginx container managed via a Kubernetes YAML file
After=network-online.target
Wants=network-online.target

[Kube]
# Path to the Kubernetes YAML file defining the pod
Yaml=/etc/containers/systemd/nginx.yml

[Install]
# Start this service automatically at boot
WantedBy=default.target
